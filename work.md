| Người | Phạm vi độc lập và file đề xuất | Đầu ra bàn giao |
|---|---|---|
| **1 — Coordinator, MCP, trace** | Sở hữu `workflow.py`, phần mở rộng cần thiết của `mcp_gateway.py`, `trace.py`; hoàn thiện [ARCHITECTURE.md](D:/nghia/vin/day8/K4-L3B-MultiAgent-MCP-A2A/ARCHITECTURE.md). Khám phá tool thực tế, tạo evidence client luôn gắn đúng `case_id`, cache theo case, giới hạn retry/call, điều phối handoff. | `solve_case()` gọi được 4 module còn lại qua mock; trace có thứ tự sự kiện và `tool_result_consumed` gắn đúng ref. |
| **2 — Entity, customer, order/product** | Tạo `entity.py` và `order.py`. Xếp hạng hoặc loại candidate bằng MCP; xác định customer và order liên quan; lấy item, seller, product khi cần. | `entity_resolution`, `customer_context`, phần order/item/seller của `affected_entities`, cùng bằng chứng cho từng kết luận. Có ca thử candidate sai và không phân giải được. |
| **3 — Shipment** | Tạo `shipment.py`. Kiểm tra timeline giao hàng, phân biệt seller delay, logistics delay, lost/returned và nguồn mâu thuẫn. | `shipment_analysis`, shipment IDs, seller giao trễ và evidence refs. Có ca thử timeline thiếu hoặc xung đột. |
| **4 — Payment/refund** | Tạo `payment.py`. Đối soát capture, split payment, charge trùng, refund pending/failed và tổng tiền bằng BRL. | `payment_analysis`, payment references, các khoản có thể hoàn có căn cứ. **Chưa tự quyết định** số hoàn cuối cùng; chuyển facts cho người 5. |
| **5 — Policy, conflict, verifier** | Tạo `policy.py`, `conflicts.py`, `verifier.py`. Tổng hợp findings, đánh giá từng claim, chọn nguồn khi mâu thuẫn, xác định trách nhiệm, hành động, mức hoàn, confidence; kiểm tra chéo trước khi trả output. | JSON đúng schema L3B, gồm `assessment`, `claim_assessments` khi có ích, `data_conflicts`, `root_cause_analysis`, `financial_resolution`, `resolution_actions`; có test cho tính nhất quán và evidence ownership. |